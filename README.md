# Career Pairwise Lab（キャリア比較ラボ）

2択比較を通じて、キャリアの関心・判断軸・避けたい要素を整理するスマホ向けWebアプリです。適職を断定する診断ではなく、次に調べることや相談時の論点を作るためのツールです。

## 使い方

`index.html` をブラウザで開くだけで利用できます。外部API・外部ライブラリ・ビルド工程は不要で、GitHub Pagesにもそのまま配置できます。

## 主な機能

- 現在地とキャリア形態を選び、会社員・組織所属／個人活動のフローへ分岐
- 興味軸、業界・職種または活動ジャンル・収益モデル、仕事スタイルを2択比較
- 比較中はコメント入力を求めず、避けたい要素・リスクのみ選択式で記録
- 結果画面で任意の感想・気づきをメモとして残せる（未記入でも進行可）
- 結果を仮説としてランキング表示し、AI分析用プロンプトを生成・コピー
- 結果をブラウザの `localStorage` に最大30件保存し、履歴から再表示

入力内容は外部へ送信されません。保存データは利用中のブラウザ内にのみ保持されます。

## v0.4.1 の改善点

- 質問種別が変わる前に、目的・回答のコツ・進捗を示すセクション遷移画面を追加
- 比較対象の全組み合わせを出題し、ペアワイズランキングの精度を改善
- ランキングにスコア、勝敗数、比較数、勝率、選択理由を表示
- 上位候補が同点または僅差の場合、最大3問のタイブレーク質問を追加
- AI分析用プロンプトと `localStorage` の履歴に、比較統計とタイブレーク結果を保存

## v0.5 の改善点

- 候補を早期除外せず、代表比較・探索比較・接戦比較・反証比較を組み合わせる探索型比較エンジンへ変更
- 各レイヤーを最大8候補・12問に抑え、傾向適合60%・カテゴリ分散25%・探索15%を目安に出題
- 候補ランキングとは別に16種類のtraitスコアを保持し、内部傾向を推定
- 「当たり前品質」と「魅力的品質」のチェック項目をコードと保存履歴に追加
- 会社員・個人活動・創作・副業を横断する可能性マップを結果画面に追加
- trait相性は高いが比較が少ない「未探索の可能性」を表示
- 結果を鵜呑みにしないための反証質問と、次に試す小さな行動を追加

選択したキャリア形態は比較フローの分類に使いますが、候補自体の存在を消す条件には使いません。結果画面では選択モード外の可能性も参考候補として確認できます。

## v0.6 の改善点

- 体験時間を約5分、比較質問を最大20問に制御
- ウォームアップ3問、探索8問、仕事スタイル5問、接戦・反証4問の時間予算型フローへ変更
- 比較途中から現在までの回答で結果を生成できる「ここまでで結果を見る」を追加
- 上位8 traitを表示するSVGレーダーチャートを追加
- 「組織所属↔個人自律」「安定運用↔創作・挑戦」のSVGキャリア可能性マップを追加
- 総合、会社員、個人活動、創作・副業の上位候補を横棒で視覚化
- 「どちらもあり」「わからない」や僅差をまとめる迷い・接戦カードを追加
- 比較数が少なくtrait相性が高い候補を未探索候補カードとして表示

候補マスタの広さは維持し、実際の質問だけを情報量と時間予算に基づいて選びます。途中結果には、回答数が少ない仮説である旨を明示します。

## v0.7 の改善点

- 約10分でより深く探索する体験へ調整し、比較質問を最大36問に拡張
- ウォームアップ5問、探索14問、仕事スタイル8問、接戦・反証6問、タイブレーク最大3問へ変更
- 探索比較では、未知の探索候補を含む質問を最低3問確保
- 会社員・個人活動・創作・副業を横断するウォームアップと反証比較を追加
- v0.6のtraitレーダー、可能性マップ、候補バー、迷い・接戦カード、未探索候補カードを維持
- 「途中結果を見る」機能を維持し、途中結果では精度に関する注記を表示

質問数は時間予算で制御され、36問に達した時点で比較を終了します。

## v0.9 の改善点

- 初期画面を「標準版（10〜12分）」と「がっつり版（20〜30分）」の2モード化
- 標準版の回答・候補統計・trait・結果を保持したまま、追加分析としてがっつり版へ引き継ぐ機能を追加
- アンカー質問、カード仕分け、MaxDiff、接戦ペアワイズを組み合わせたハイブリッド探索フローを実装
- がっつり版に、安定と自律など8軸のトレードオフ比較を追加
- がっつり版の最後に、驚き・違和感・不安・次の行動を残す任意の内省質問を追加
- 履歴画面に「標準版」「がっつり版」「標準版 → がっつり版」のモードと関連を表示
- 保存形式をv0.9へ更新し、引き継ぎ元セッションIDと質問形式別ログを保存

標準版からの引き継ぎ分析では、標準版と同じ質問を繰り返さず、接戦候補・未探索候補・トレードオフを追加で確認します。

## v0.9.1 の改善点

- アンカー、カード仕分け、MaxDiff、ペアワイズ、トレードオフ、内省、避けたい要素の全形式に「戻る」を追加
- 回答ログを正として候補スコアとtraitを毎回再計算し、回答変更時の二重加算を防止
- MaxDiffのBEST・中間・WORSTから、重み付き疑似ペアワイズ比較を最大5本生成
- 候補スコアに比較回数ベースの信頼度補正と事前分布を追加
- 結果表示を0〜100の `displayScore` に統一し、少数回答の候補を50点付近へ補正
- raw traitを回答数で正規化し、レーダーチャート用スコアを5〜95に制限
- 結果に比較回数、信頼度、Best回数、Worst回数を表示

戻る操作では直前ログを取り消してから全スコアを再構築するため、回答を変更しても古い加点は残りません。

## v0.9.4 の改善点

- 初期質問の「キャリア形態」を、現在の状態ではなく「希望する／気になるキャリア形態」へ変更
- 現在地の選択と、これから比較したい探索方向を別画面へ分離
- 希望するキャリア形態を1〜3件の複数選択に変更
- 「まだ決めずに広く見たい」を追加
- 希望するキャリア形態は候補の除外条件にせず、MaxDiff候補セットの配合優先度にのみ利用
- 結果画面とAI分析用プロンプトに、今回選んだ探索方向と用途を表示
- 保存形式へ `desired_career_modes` を追加し、旧`career_mode`履歴も読み込める互換処理を追加

特定のキャリア形態を選んでも、会社員・個人活動・創作・副業など他方向の候補は引き続き比較対象へ含まれます。

### v0.9.4 動作復旧・バグ修正

`desired_career_modes` 対応の直後、質問に1問回答した時点でアプリが応答不能になる不具合が発生していました。原因と対応は以下の通りです。

- **原因**: 過去のタイブレーク機構の残骸である `S.tieLogs` が、状態初期化コード（`S={...}`／`reset()`）から削除された後も `headToHead()` と `reasonsFor()` の中で `[...S.logs,...S.tieLogs]` として参照され続けていた。`S.tieLogs` が `undefined` のためスプレッド構文が例外を送出し、最初の回答直後に呼ばれる `recalculateAllScores() → ranking() → reasonsFor()` の経路で必ずクラッシュしていた。
- **対応**: 現行アーキテクチャでは全回答ログが `S.logs` に一本化されているため、`[...S.logs,...S.tieLogs]` を `S.logs` に修正（`tieLogs` 自体は廃止済みの概念のため状態への再追加はしていない）。
- **副次修正**: `history()` が保存データ内の `null` や必須フィールド欠落レコードをフィルタせずそのまま返しており、壊れた `localStorage` 履歴が1件でも混ざると履歴画面が丸ごとクラッシュする経路があった。`history()` で `id` を持つオブジェクトのみを残すようフィルタを追加し、`showHistory()` 側の `user_status` 参照もオプショナルチェーンで防御化。
- **互換性**: `desiredModesOf()` は `desired_career_modes`（新形式）と旧 `career_mode`（文字列／オブジェクト）の両方を読み替えるため、v0.6〜v0.9のどの履歴形式でも結果画面・履歴一覧・がっつり版への引き継ぎが破綻しないことを確認済み。
- **確認方法**: `node --check` によるスクリプト構文チェック、および実ブラウザでの標準版・がっつり版それぞれの単独完走、標準版→がっつり版引き継ぎ、戻る操作、旧形式・破損データ混在の履歴表示を一通り動作確認。

## v0.9.5 の改善点（安定化・設計整合・戻る機能改善）

新機能追加ではなく、設計思想と実装フローのズレの是正、旧コード整理、再発防止を目的とした改修です。

- **カード仕分けを主フローから除外**：標準版・がっつり版・引き継ぎ分析のいずれの `phases()` 配列からも `'cardSort'` を削除し、標準版は「アンカー→4択MaxDiff/Best-Worst→接戦ペアワイズ→避けたい要素」、がっつり版はこれに「トレードオフ比較→内省質問」を加えた構成に統一。カード仕分け専用画面 `cardSortScreen()` と、`startPhase()`／`goBack()` 内の対応する分岐は到達不能となったため削除。ただし `cardSortCandidates()`（MaxDiff候補プール選定に使用）と `S.cardSortLogs` のスコア再計算ロジックは、v0.9.4以前に保存された履歴を継続分析する際の互換性のために維持。
- **標準版を4択MaxDiff主軸に整理**：`MODE_CONFIG` を `anchor:8 / maxDiff:18 / pairwise:6 / avoid:1`（標準版）、`anchor:12 / maxDiff:32 / pairwise:14 / tradeoff:10 / avoid:1 / reflection:5`（がっつり版）、`additionalMaxDiff:12 / closePairwise:10 / tradeoff:8 / reflection:4`（引き継ぎ分析）に再編。旧フェーズ名 `conjoint` は `tradeoff` に統一し、引き継ぎ分析からは使われていなかった `additionalAnchor` ステップを削除。
- **戻る機能を選択画面まで拡張**：既存の回答ログベースの `answerTrail` に加え、`S.navigationStack` を追加。希望キャリア形態選択画面からアンカー質問へ進む際にスクリーン復帰用のエントリを積んでおき、アンカー質問1問目（未回答で `answerTrail` が空の状態）で「戻る」を押すと、選択済み内容を保持したまま希望キャリア形態選択画面へ戻れるようにした。現在地選択・モード選択への戻るは元々ハードコードされた遷移で正しく機能していたため変更なし。戻る処理はスコアを直接減算せず、対象ログを削除したうえで `recalculateAllScores()` により再計算する既存方針を維持。
- **旧コード・到達不能コードの整理**：現行フローから到達不可能と確認した `hybrid()` / `hybridOrCareer()` を削除。`names` / `intros` / `bucketFor()` に残っていた `cardSort` / `additionalAnchor` 用のエントリも合わせて整理し、`intros` のステップ番号をカード仕分け除去後の並びに合わせて振り直した。
- **`tieLogs` 再発防止**：`S.logs` に全回答ログを一本化する方針をコードコメントとして明記し、`tieLogs` を含む廃止済みログ配列への参照が残っていないことを再確認済み。
- **`desired_career_modes` 互換性の再確認**：`desiredModesOf()` が新形式（配列）・旧 `career_mode`（文字列／オブジェクト）のどちらでも読み替えられること、`career_mode.id` への直接参照が `branchOf()` 内の1箇所のみで null-safe に保たれていること、候補除外ではなくMaxDiff配合調整にのみ使われていることを再確認。
- **履歴データの堅牢化**：`history()` が配列以外や `id` を持たないレコード（`null`・文字列・空オブジェクトなど）を除外するフィルタを追加し、`showHistory()` 側の `user_status` 参照もオプショナルチェーンでフォールバック（`不明`）するよう防御化。破損データが混在していてもアプリ本体・履歴画面ともにクラッシュしないことを確認。

大規模なリファクタは行わず、既存のスコア計算方式（`S.logs` を正としたログ再計算）・保存形式・AI分析用プロンプト構成は維持しています。

## v0.9.6 の改善点（友人フィードバック反映）

実際に触ってもらった友人からのフィードバックをもとに、体験品質を見直しました。

- **MaxDiffの選び直し対応**：4択画面を、各候補カードに「一番気になる」「一番違う」の2ボタンを持つ方式へ刷新。次へ進む前ならどちらのボタンも自由に押し直せるようにし、同じ候補を両方には選べないよう相互排他制御を追加（片方に選ぶと、もう片方の選択が自動解除される）。画面上部に現在の選択状態を常時表示し、未選択の場合は具体的な案内文を表示する。
- **比較中のコメント欄を全廃止**：アンカー・MaxDiff・ペアワイズ・トレードオフの比較画面から「理由を一言」入力欄を削除。質問数が多いフローでの入力負荷をなくし、比較そのものに集中できるようにした。
- **がっつり版の内省質問を主フローから除外**：結果直前に内省質問4問を強制する構成をやめ、`phases()` からは完全に除去。代わりに結果画面の末尾に「感想・気づきを任意でメモする」という折りたたみ式パネルを追加し、書いても書かなくても結果を確認できるようにした。メモは保存済み履歴レコードへ追記できる。
- **文言のトーン統一**：「診断」「あなたは〇〇タイプ」のような断定的表現を避け、「探索用の仮説」「関心が寄っている可能性があります」という控えめなトーンに統一。現在地・希望キャリア形態・アンカー・MaxDiff・ペアワイズ・トレードオフ・結果画面の説明文を指定の言い回しに揃えた。
- **候補ジャンルの拡張**：会社員・組織所属系（営業企画、物流企画、学校職員、インフラ運用、福祉専門職など）、個人活動・小商い系（カフェ・菓子製造、セレクトショップ、パーソナルトレーニング、イベント主催、民泊・宿泊運営、ペット関連サービス、コーチング・相談業など）、クリエイター・発信系（漫画・同人活動、SNS発信、VTuber・キャラクター活動、ゲーム制作、ボイス・ナレーション、舞台・イベント表現、ブログ・メディア運営など）、起業・事業づくり系（SaaS・Webサービス、AI活用サービス、コミュニティ運営、フランチャイズ経営、代理店ビジネスなど）を候補マスタに追加。質問数は増やさず、MaxDiff候補セット生成のカテゴリ分散・探索枠に混ぜて広く見せる。
- **希望キャリア形態の影響を可視化**：希望キャリア形態を選んだ後、MaxDiff開始前の遷移画面に「今回やや多めに出す候補」を具体的に表示し、候補の除外ではなく配合調整であることを明記。結果画面のパネル名も「今回選んだ探索方向」に統一し、同じ趣旨の説明を添えた。
- **UI/操作感の改善**：MaxDiffのカードにボタンとステータス表示を追加し、選択状態が一目で分かるようにした。パネルや選択肢の余白・タップ領域を拡張し、AI分析用プロンプトを折りたたみ表示に変更して結果画面の見通しを改善した。
- **不具合の再チェック**：戻る操作、MaxDiffの選び直し、Best/Worstの同一候補選択防止、標準版・がっつり版・引き継ぎ分析それぞれの完走、壊れたlocalStorage履歴が混在した場合の表示、AIプロンプトのコピーなどを実ブラウザで再確認。この過程で、がっつり版の「避けたい要素」画面の次へボタンが内省質問の廃止後も「内省へ」という古いラベルのままだった点を発見し、「結果を見る」へ修正した。

localStorageの保存バージョンを `0.9.6` に更新しました。旧バージョンの履歴（`0.9`〜`0.9.5`）は引き続き読み込み・表示できます。

## v0.9.7 の改善点（「向いてない可能性」の可視化）

これまで結果画面は「関心が寄っている可能性（向いてる方向）」のランキングと可視化のみで、MaxDiffで「一番違う（Worst）」に選ばれた候補は内部スコアとしてのみ保持され、画面には出ていませんでした。v0.9.7では、この「避けたい傾向」もあわせて表示します。

- **「今は距離を置きたい傾向」セクションを追加**：`insightFor()` に `avoidLeaning` を追加し、`scoreCandidates()` の結果から `worstCount`（MaxDiffで「一番違う」に選ばれた回数）が1回以上ある候補を、`worstCount` 降順・`displayScore` 昇順で最大7件抽出。結果画面の「未探索候補」パネルの直後に、「向いていないと断定するものではなく、今の自分の関心が向かいにくい方向として参考にしてください」という注記つきで表示。該当候補が0件の場合はパネル自体を出さない。
- **「フィットしにくいかもしれない環境（仮説）」セクションを追加**：16種類のtraitそれぞれについて、そのtraitが高い人が置かれやすい環境を表す `traitMisfitHints` を定義。`displayTraits`（0〜100に正規化済みのtraitスコア）を昇順に並べ、スコアが低い上位4traitに対応する環境仮説を表示する。「traitスコアが低い＝そのtraitが強く求められる環境とは噛み合いにくい」というロジックで、断定を避けた見出しと注記を添えている。
- **AI分析用プロンプトへの反映**：`promptFor()` に、上記の「距離を感じている候補（仮説）」と「フィットしにくいかもしれない環境（仮説）」を追記。あわせて、AIへの依頼文にも「距離を感じている候補や環境についても、決めつけずに仮説として深掘りする問いを含めてください」という一文を追加した。
- **保存形式**：localStorageの保存バージョンを `0.9.7` に更新。`avoidLeaning` / `misfitEnvironments` は `record()` 内の `insight` を経由して各履歴レコードの `insights` に含まれるため、新規追加のフィールドを保存形式に個別追加する必要はない。旧バージョンの履歴（`insights` を持たない、または本セクションを含まない形式）を開いた場合は `insightFor(r)` が都度再計算するため、表示側は後方互換。

「向いてる方向」と同様に、この「避けたい傾向」もあくまで今回の回答に基づく仮説であり、断定的な適性診断ではありません。

## v0.9.8 の改善点（UI/UX全体改善）

「使い始めてすぐに面白いと感じ、最後まで完走できるUI」を目指し、スタイルとHTML構造のみを対象に見た目を刷新しました。スコア計算・出題ロジック・保存形式のフィールド構成など、既存のJS/ロジックは変更していません。

- **トップ画面の第一印象を強化**：キャッチコピーを「自分でも気づいていない、働き方の傾向を探ろう。」に変更し、リード文を3行以内に整理。標準版・がっつり版の各モードカードに所要時間バッジ（⏱ 10〜12分／⏱ 20〜30分）を追加し、余白を拡大。
- **進捗バーに現在フェーズと残り目安を追加**：画面上部の固定プログレスバー（`.progress-shell`）にラベル行を新設し、`progress()` が `S.phase` から「現在：アンカー質問」等のフェーズ名と「あと目安n問」を表示するように拡張。バーの太さ・配色も見直し。
- **MaxDiff（4択）カードのデザインを強化**：選択状態表示を「★ 選択中：〇〇」「✕ 選択中：〇〇」に変更し、各カードのボタンラベルを「一番気になる ★」「今は違う ✕」に統一。選択済みカードの背景・枠線色をより明確なコントラストに調整。
- **結果画面の情報設計を改善**：総合上位候補パネルを「関心が寄っている可能性」に改称し、上位5件に番号バッジを追加。「今は距離を置きたい傾向」パネルをその直後に配置し、緑（関心）とオレンジ（距離を置きたい）の左ボーダーで視覚的に対比。traitレーダーの説明文に「この図は16の傾向スコアのうち上位8つを表しています」を追記。AI分析用プロンプトの折りたたみラベルを「AIに深掘りしてもらう」に変更（デフォルト非表示は維持）。「もう一度やる」ボタン文言に変更。
- **全体スタイルを統一**：本文16px・行間1.7を基準に、見出しサイズ・カードの角丸（`--radius`/`--radius-lg`）・ボタンpadding・セクション間余白を統一。プライマリカラーを緑1色（`--primary`）に整理し、オレンジ（`--danger`）は警告・Worst系のみに限定。ビューポートを`max-width:480px`でセンタリングし、スマホ特有の詰まった印象を解消。
- **保存バージョン**：localStorageの保存バージョンを `0.9.8` に更新。データ構造・フィールドの追加はなく、表示側の改善のみのため旧バージョンの履歴もそのまま表示できます。

## v0.9.9 の改善点（候補の説明文を追加）

候補（会社員向けの職種52種・個人活動向けの活動ジャンル37種、合計89件）が名前だけでは内容を想像しにくいという課題に対応し、それぞれに短い説明文と詳しい説明文を追加しました。

- **候補マスタに `desc` / `detail` を追加**：`CANDIDATE_TEXT` オブジェクトに、`D.job`（52件）・`D.solo`（37件）の全候補分の説明文を `job:` / `solo:` プレフィックス付きIDで定義し、`D.job` / `D.solo` の生成処理でマージ。`desc` は「〜する仕事」「〜を扱う仕事」で終わる20〜35文字程度の一文、`detail` は「〜に向く可能性があります」など断定を避けた表現で仕事内容を50〜90文字程度で説明する二文構成。
- **MaxDiff（4択）カードに `desc` を表示**：候補名の下、選択ボタンの上に `desc`（`.candidate-desc`、13px・`var(--muted)`）を追加表示。
- **結果画面の「関心が寄っている可能性」「今は距離を置きたい傾向」に `detail` を表示**：候補名の下に `detail`（`.candidate-detail`、13px・`var(--muted)`・やや薄め）を1〜2行で表示。「会社員寄り候補」等の他のランキングパネルには表示せず、この2セクションに限定。

### v0.9.9 コード整形・リファクタリング実施

機能・挙動・スコア計算・localStorageのデータ構造やバージョン文字列は一切変更せず、`index.html` 内のCSS/JSを整形・リファクタリングしました。

- **整形**：インデントを2スペースに統一し、1行に詰め込まれていたCSS/JSを改行・インデントで展開。CSSプロパティをlayout→box→typography→color→effectの順に並べ替え。要所に日本語コメントを追加。
- **デッドコード削除**：呼び出し箇所のなかった `ranks(title,items)` 関数を削除。
- **関数分割**：巨大な1関数だった `showResult()` を、`renderResultHead` / `renderTraitRadarPanel` / `renderPossibilityMapPanel` / `renderAvoidLeaningPanel` / `renderCloseContestsPanel` / `renderUnexploredPanel` / `renderMisfitPanel` / `renderAvoidFactorsPanel` / `renderNextActionsPanel` / `renderPromptPanel` の10個のパネル生成関数に分割。
- **マジックナンバーの定数化**：`SCORE_DISPLAY_MIN/MAX/BASE`、`CANDIDATE_CONFIDENCE_SMOOTHING`、`CANDIDATE_SCORE_SCALE`、`TRAIT_CONFIDENCE_SMOOTHING`、`TRAIT_SCORE_SCALE`、`MAX_HISTORY`、`TOAST_DURATION_MS`、`REFLECTION_MAX_LENGTH`、`TOP_N_DISPLAY` を定数化。
- **CSS変数の追加**：3箇所以上で重複していたハードコード値を `--hairline`（罫線色）・`--shadow-soft`（薄い影色）・`--radius-pill`（ピル型の角丸）としてCSS変数化。
- **検証**：`node --check` による構文チェック、テンプレート文字列の再構成一致検証、`showResult()` 分割前後の出力一致テスト（複数パターン）、CSS宣言の意味的等価性検証（変数解決込み）、標準版・がっつり版のフル自動走行、MaxDiff選択時のスクロール非リセット確認を実施し、いずれも差分なしを確認。
- **保存バージョン**：localStorageの保存バージョンを `0.9.9` に更新。`desc` / `detail` は `D.job` / `D.solo` からその都度マージされる表示用フィールドのため、保存データ構造への影響はなく旧バージョンの履歴もそのまま表示できます。

### v0.9.9 追加の安定化・セキュリティ修正

バージョン表記は `0.9.9` のまま、リファクタリング直後に見つかった不具合と脆弱性を修正しました。

- **XSSエスケープ漏れの修正**：`esc()` が `"` / `'` をエスケープしておらず、履歴一覧の日時・ステータスラベル、結果画面の避けたい要素チップ、次に試す小さな行動リストなど `esc()` を通さずテンプレートへ直接差し込んでいた箇所が複数残っていた。`esc()` を `"` / `'` も含めてエスケープするよう強化し、該当箇所すべてに適用。
- **MaxDiff選択時のスクロールリセットを修正**：4択画面でボタンを押すたびに `screen()` で画面全体を再描画していたため、選び直すたびにスクロール位置が先頭へ戻っていた。選択状態の差分だけをDOM更新する `update()` 関数を新設し、`render()` は初回描画のみ・以降の操作は `update()` を呼ぶ構成に変更してスクロール位置を保持。
- **未使用配列の削除**：実行時コードから参照のない開発用チェックリスト `expectedQualityChecks` / `attractiveQualityChecks` を削除。

## v1.0.0 の改善点（機能追加・UX改善・コーチ導線）

- **結果の持ち出し機能を追加**：結果ページに「結果をコピー」「結果をテキストで保存」ボタンを追加。コピーは候補TOP3・傾向TOP3の要約、テキスト保存は候補TOP・傾向TOP・AIプロンプトを含むメモをダウンロードする。
- **候補フィルターを追加**：`D.job` / `D.solo` の各候補にカテゴリタグ（`category`）を付与し、スタート画面（モード選択前）に「候補を絞り込む（任意）」パネルを追加。選んだカテゴリの候補だけを対象に出題するが、フィルター後の候補数が `CATEGORY_FILTER_MIN_POOL`（8件）未満になる場合は探索が成立しないため自動的に未フィルター扱いに戻す。希望キャリア形態と同様に除外条件ではなく出題の絞り込みとして扱い、結果集計（`careerPool(false)`）は常に全候補を対象にする。
- **履歴画面にスコア推移グラフを追加**：過去の結果から上位3候補のスコア推移を示すSVGグラフを追加。
- **フェーズ内進捗バーを追加**：MaxDiff・接戦ペアワイズの画面上部に「現在n問目 / 全m問」の進捗バーを追加。
- **モバイル向けレスポンシブ調整**：タップサイズ・余白の拡大、`max-width:600px` でのフォントサイズ・パネルpadding・アクション配置（1カラム化）を調整。
- **キャリアコーチ相談導線を追加**：結果ページ最下部に相談導線パネルを追加。当初は「プロのキャリアコーチに壁打ち」という訴求だったが、実態に合わせて「作者に相談する」という作者相談スタイルの文言へ修正。
- **バージョン表記を更新**：タイトル・保存バージョン（`version`）・結果画面のラベルを `1.0.0` に更新。

## v1.0.1 の改善点（初期絞り込みUI改善・業界・業種／職種フィルター追加）

現在地・希望キャリア形態を選んだあとに「探索範囲を選ぶ」画面を追加し、業界・業種（16種）と職種（15種）を任意で指定できるようにしました。希望キャリア形態と同じく、これは候補を除外する条件ではなく、MaxDiff候補セットの配合を調整する探索設定として扱います。

- **「探索範囲を選ぶ」画面を追加**：希望キャリア形態選択のあと、比較フロー開始前に新しいステップとして挿入。「業界・業種を選ぶ（最大5つ）」「職種を選ぶ（最大5つ）」の折りたたみ式チェックリストと、「探索の広さ」（広め／バランス／強め、デフォルトはバランス）の3択、「まだ広く見たい（絞らずに広めに比較する）」のワンタップ導線、選択内容をリアルタイム反映する「今回の探索設定」サマリーで構成。戻る操作は希望キャリア形態選択画面・比較フロー開始後の双方から正しく行き来できるよう `S.navigationStack` の分岐を拡張。
- **業界・業種フィルター（`INDUSTRY_FILTERS`）を追加**：IT・Web、AI・データ、人材・採用・キャリア、教育・研修、医療・福祉・ヘルスケアなど16カテゴリを新設。
- **職種フィルター（`ROLE_FILTERS`）を追加**：営業・顧客支援、マーケ・広報、企画・事業づくり、IT・開発、発信・表現活動、個人事業・小商い、起業・経営など、会社員職種と個人活動・クリエイター寄りの役割を横断する15カテゴリを新設。
- **候補マスタにタグを付与**：既存の `D.job`（8カテゴリ）・`D.solo`（7カテゴリ）の `category` から `industry_tags` / `role_tags` / `career_mode_tags` を導出するマッピングテーブルを追加し、候補89件全件に自動付与。個別候補ごとの手動タグ付けを行わずに、既存の分類情報から一貫したタグ付けを実現。
- **MaxDiff候補セット生成にフィルターを反映**：`scoreCandidateByFilters()` で業界・職種・希望キャリア形態タグの一致度を採点し、「探索の広さ」ごとの配合比率（広め：近い候補30%／カテゴリ分散40%／未探索20%／反証10%、バランス：50/25/15/10、強め：70/15/10/5）で候補プールを構成する `cardSortCandidates()` の配合ロジックを実装。フィルター未選択時は「広め」と同じ扱いになり、候補が完全に除外されることはない。
- **結果画面・AI分析用プロンプトに探索設定を表示**：「今回の探索設定」パネルに、希望する・気になるキャリア形態に加えて業界・業種フィルター、職種フィルター、探索の広さを表示。AI分析用プロンプトにも同内容と「候補除外ではなく配合調整に使っている」旨を追記。
- **がっつり版引き継ぎへの継承**：標準版からがっつり版への引き継ぎ分析でも、選択済みの探索設定（業界・業種・職種・広さ）をそのまま引き継ぐ。
- **保存形式**：localStorageの保存バージョンを `1.0.1` に更新。`exploration_filters`（`industry_ids` / `role_ids` / `strength`）を履歴レコードに追加。旧バージョンの履歴（`exploration_filters` を持たない形式）を開いた場合は `industry_ids: []` / `role_ids: []` / `strength: "broad"` をデフォルトとして扱うため、後方互換性は維持される。

## v1.0.2 の改善点（レビュー反映の小修正まとめ）

プロダクトレビューで挙がった離脱ポイント・導線の迷い・デッドコードのうち、小さく直せるものをまとめて修正しました。

- **「まだ広く見たい」を即遷移に変更**：探索範囲画面のワンタップ導線が「選択をクリアして同じ画面を再表示する」挙動だったのを、広め設定（フィルターなし・`strength: "broad"`）でそのまま比較フローを開始する挙動に変更。ラベルも「まだ広く見たい（絞らずにこのまま始める）」に更新。
- **MaxDiff画面に「途中結果を見る」を追加**：最長フェーズであるMaxDiff中に離脱の逃げ道がなかったため、画面下部のsticky領域に「途中結果を見る」導線を追加（既存の `earlyResult()` を使用）。
- **結果画面の順序を再編**：ユーザーが最初に知りたい「関心が寄っている可能性」（候補TOP）を結果ヘッダー直後に移動。traitレーダー・キャリア可能性マップ・「今回の探索設定」パネルは折りたたみ（`<details>`）化して下部へ移動。スコアの免責注記は上部に残した。
- **所要時間表記を統一**：ホーム画面バッジと `MODE_CONFIG` の不一致を解消し、標準版「12〜15分」・がっつり版「25〜35分」に統一。
- **避けたい要素画面の戻る操作で選択を復元**：`goBack()` のavoid分岐で選択が全消去されていたのを、`entry.payload.selected` から復元するよう修正。
- **デッドコード削除**：一度も呼ばれていなかった `workStylePairs` / `contrastPairs` / `tiebreakPairs` / `needsTieBreak` / `restartPhase` の5関数と、常に空文字だった比較理由（`reason`）の記録・集計・プロンプト出力経路を削除（約130行）。理由の記録はv1.1でワンタップ理由タグとして再設計予定。
- **バージョン表記を更新**：タイトル・保存バージョン・結果画面ラベルを `1.0.2` に更新。保存形式の変更はなし。

## v1.1.0 の改善点（途中再開・理由タグ・JSON入出力・未探索チェック・ホームフィルター刷新）

- **途中セッションの自動保存＆再開**：回答・戻る操作・画面遷移のたびに進行状態を `careerPairwiseLabSession` キーへ自動保存。次回起動時のスタート画面に「前回の続きがあります」パネル（続きから再開／破棄して最初から）を表示する。診断完了時・「最初から」実行時に自動削除。
- **接戦ペアワイズにワンタップ理由タグを追加**：接戦ペアワイズ（`pairwise` / `closePairwise`）でどちらかの候補を選ぶと、「収入・面白さ・人間関係・成長・安定・自由度」の6タグから理由を1つ選べる画面を挟む（スキップ可・フリーテキストなし）。選んだタグは `reason_tag` としてログに保存され、AI分析用プロンプトの「接戦比較で選んだ理由タグ」欄に反映される。
- **結果のJSONエクスポート／インポート**：結果画面に「結果をJSONで保存」ボタン、履歴画面に「JSONファイルから読み込む」ボタンを追加。端末間の結果移動や、作者相談時のデータ共有に使える。同じIDの結果は上書きされる。
- **「未探索候補をあと5問比べる」ループを追加**：結果画面の未探索候補パネルから、まだ比較していない候補を中心にした4択比較（5問）を開始できる。既知の上位候補を基準点として混ぜた候補セットで比較し、完了すると更新された結果が新しい履歴（`explore_more`）として保存される。
- **ホームの候補フィルターを「探索範囲の事前指定」に刷新**：カテゴリ単位の絞り込みを廃止し、業界・業種（16種）と仕事の種類（15種）の2軸指定に変更。位置づけを「最初から範囲を決めたい人向け。やり直したくなったら『最初から』でいつでも広げ直せます」に改め、STEP3の配合式フィルターとの違い（こちらは比較対象そのものを絞る／STEP3は配合調整）を明記。探索範囲指定中はフェーズ遷移画面の文言も「最初に選んだ探索範囲の中で、候補を早期除外せず〜」に変わる。絞り込み後の候補が8件未満になる場合は従来どおり全候補に戻る。
- **STEP3のラベル変更**：「職種を選ぶ（最大5つ）」を「探索したい仕事の種類（最大5つ）」に変更（会社員職種と個人活動を横断するリストであることを明確化）。
- **保存形式**：保存バージョンを `1.1.0` に更新。ペアワイズログに `reason_tag`（null許容）を追加。既存履歴との後方互換性は維持される。

## v1.2.0 の改善点（候補データ拡充・候補ごとの個別タグ付け）

- **候補を89件から132件に拡充**：日本の就活・転職市場の実態に合わせ、会社員職種を52→80件（+28件：IT営業、MR・医療営業、不動産営業、金融営業、セールスエンジニア、バイヤー、店舗開発、MD、Webディレクター、SNS運用、CRM、セキュリティ、クラウド・インフラ、アプリ開発、データ基盤、労務、内部監査、IR、貿易事務、施工管理、ゲームプランナー、UXリサーチャー、空間設計、看護・リハビリ、保育、行政職公務員、学術研究、心理・カウンセラー）、個人事業・起業系を37→52件（+15件：小さなバー、レンタルスペース、家事代行、司会・MC、子育て支援、翻訳・通訳、デザイン受託、動画編集受託、経理・事務代行、オンライン秘書、ストック素材販売、定期便サブスク、輸入・越境EC、地域ガイド、ワークショップ運営）に拡充。全新規候補に短い説明文（`desc`）・詳しい説明文（`detail`）・trait重み（`traitWeights` の個別エントリ）を付与。
- **全候補への個別タグ付け（フィルター解像度の根治）**：カテゴリ一括導出だった `industry_tags` / `role_tags` を、`JOB_TAGS` / `SOLO_TAGS` による候補ごとの個別付与に刷新。16業界×15職種のフィルターが候補単位で実際に識別力を持つようになった（例：同じ `tech_data` カテゴリでも「セキュリティエンジニア」と「データ基盤エンジニア」で業界タグが異なる）。カテゴリ由来のマッピングは、タグ未定義の候補を将来追加した場合のフォールバックとして残置。
- **検証**：全132候補について、タグの網羅性（業界・職種・キャリア形態タグが全候補に存在）、タグIDの有効性（16業界・15職種の定義と一致）、説明文の完備、ID重複なし、全16業界・全15職種フィルターがそれぞれ3件以上の候補にヒットすることをスクリプトで確認済み。
- **保存形式**：保存バージョンを `1.2.0` に更新。旧履歴との後方互換性は維持される。

## v1.2.1 の改善点（ペアワイズ勝利の重みを減衰）

- **2択比較1勝の重みを +3 から +1.5 に減衰**：ペアワイズ1勝（+3/比較1回）とMaxDiff best（実効約+0.67/回答）の非対称により、MaxDiffで明確に優勢な候補がペアワイズ1問の負けで同点まで詰められ、直接対決タイブレークで逆転しうる問題を修正。4パターンのシミュレーションで、(1)一貫した回答・複数問での逆転はどの重みでも同じ結果になること、(2)単発の逆転回答だけが +1.5 では順位を動かさなくなることを確認した上で採用。定数は `PAIRWISE_WIN_SCORE` として `applyPair`（履歴再計算）と `applyAnswer` の両方で共通使用。
- **バージョン表記を更新**：保存バージョンを `1.2.1` に更新。保存形式の変更はなし（過去履歴も再計算時に新しい重みで評価される）。

## v1.3.0 の改善点（内部リファクタリング — 挙動変更なし）

機能追加の前段として、単一HTMLファイル内部を8セクション構成に再編しました。**外部から見た挙動・スコアリング・保存形式に変更はありません。**

- **セクション再編**：`CSS（コンポーネント別コメント区分）→ DATA（候補・タグ・設定の純粋データ）→ ENGINE（スコアリング・ペア生成などDOM非依存ロジック）→ STATE（セッション状態・保存・復元）→ SCREENS（画面ごとに1関数）→ NAVIGATION（遷移・戻る）→ EXPORT/IMPORT → BOOT` の順に整理。
- **デコレーターチェーンの解消**：`showResult`（6重）・`showHistory`（3重）・`home`（2重）・`pair`（3重）・`maxDiffScreen`・`promptFor`（2重）の段階的上書きを廃止し、各画面を最終DOMを直接生成する単一関数に統合。DOM順序・イベント・文言は完全に維持。
- **マジックナンバーの定数化**：trait反映重み（勝ち/どちらもあり/どちらも微妙）、MaxDiff trait重み、希望キャリア形態ボーナス、フィルター一致加点、接戦判定の点差、各表示件数、信頼度しきい値、最大選択数などを名前付き定数としてDATAセクションに集約。値はすべて従来と同一。
- **状態生成の共通化**：3箇所に重複していた状態オブジェクト生成を `freshState()` / `stateFromRecord()` に統合（リセット・がっつり引き継ぎ・未探索チェックで共用）。
- **localStorage操作の一元化**：`storageGet` / `storageSet` / `storageRemove` のtry-catchラッパーに集約し、履歴・セッション・インポートの全読み書きを経由させた。
- **デッドコード削除**：未呼び出しの `applyAnswer` / `updateTraits` / `backPair` / `renderResultHead`（統合により不要化）と、未使用CSSセレクタ4種（`.rank-reason` とカード仕分け時代の `.sort-list` / `.sort-row` / `.sort-actions`）を削除。
- **検証**：固定回答シーケンス（アンカー6問・MaxDiff5問・接戦4問・トレードオフ2問・理由タグ含む）に対する全候補スコア・順位・trait・インサイト・AIプロンプトがリファクタ前後でバイト単位一致することをスクリプトで確認。加えて132候補のデータ整合性検証、Chrome実機での主要フロー（旧バージョン保存セッションの再開互換含む）を確認済み。

## v1.4.0 の改善点（自己分析の深化 — 判断軸メタ分析・反証モード・時系列比較）

- **判断軸メタ分析（B-1）**：接戦ペアワイズの理由タグ（`reason_tag`）をセッション単位で集計し、結果画面に「接戦で選んだ判断軸（仮説）」パネルを追加。繰り返し選んだ軸の上位2つ（回数付き）と一度も選ばなかった軸を表示し、「判断軸が『◯◯』に寄っている可能性（仮説です）」と偶然の偏りへの注意書きを添える。タグ回答が2件未満の場合はパネル自体を非表示。集計は `reason_tag_summary` として履歴レコードに保存され、AI分析用プロンプトの「判断軸メタ分析」欄にも反映される。
- **反証モード（B-2）**：結果画面に「この結果を疑ってみる（2分）」ボタンを追加（任意・途中でやめて結果に戻れる）。1位候補について、①合わないとしたらの仮説（候補の `trait_weights` 上位traitの `traitMisfitHints` から導出、4つに満たない場合は汎用弱点仮説で補完）＋「思いつかない」、②2位候補ではなく1位を選ぶ決め手（理由タグ6種＋「説明できない」）、③まだ調べていないこと（収入の実態・一日の仕事内容・必要なスキル・働く人の声、複数選択可）の3問をワンタップで実施。回答は「反証チェックの記録」パネルとして結果に追記され、履歴レコード更新とともにAIプロンプトの「反証チェック」欄へ反映。「思いつかない」「説明できない」を選んだ場合は「それも大事な発見です。言語化できない魅力や不安は、実際に人に会って確かめる価値があります」を表示。
- **時系列比較（B-3）**：履歴が2件以上ある場合、結果画面と履歴画面に「前回との比較」パネルを追加。上位5候補の順位変動（↑n／↓n／→／NEW）、最大上昇候補についての文言（「◯◯前と比べて『◯◯』が◯つ上がりました。興味が動いているのかもしれません」）、判断軸タグの変化（両セッションにタグが2件以上ある場合のみ）を表示。異なるモード間（標準版／がっつり版／未探索チェック）の比較は順位変動のみとし、スコア差は比較しない旨を明記。
- **検証**：タグ集計（上位・未選択・2件未満の非表示）、順位比較（↑↓NEW・期間表記・タグ変化・異モードフラグ・前回なし）、プロンプト反映（集計あり/なし・反証あり/なし）のロジックをヘッドレス検証スクリプトで確認。Chrome実機で履歴画面の比較パネル、反証フロー3問の完走（サポート文言・2位候補参照・記録パネル）、判断軸パネルの表示を確認済み。
- **保存形式**：保存バージョンを `1.4.0` に更新。履歴レコードに `reason_tag_summary`、反証実施時に `falsification_check` を追加。旧履歴との後方互換性は維持（集計は表示時に再計算でフォールバック）。

## v1.5.0 の改善点（行動への接続 — 行動ガイド・求人票照合・危険シグナルの問いリスト）

- **行動ガイド（C-1）**：DATA層に `ACTION_GUIDE` を新設し、全132候補に「検索キーワード3つ＋その仕事をしている人に聞く質問2つ」を定義（その職種を知らない学生が読んでそのまま行動に移せることを品質基準に、候補ごとに個別執筆）。結果画面に「上位候補を調べる・聞く」パネルを追加し、上位3候補それぞれをタップで展開できる。「答え合わせではなく、仮説の検証です」の位置づけを明記。
- **求人票照合プロンプト（C-2）**：結果画面に「求人票と照らし合わせる」パネルを追加。ボタンで診断サマリー（現在地・希望キャリア形態・候補TOP5・判断軸・避けたい要素＋確認したい問い）と照合指示文（「断定せずに指摘」「企業を批判せず、ユーザーが確認すべき問いのリストとして出力」）を含むプロンプトをコピーできる。使い方は「①コピー ②ClaudeやChatGPTに貼る ③続けて気になる求人票を貼る」の3ステップで表示。求人票の本文はアプリでは扱わない。
- **危険シグナルの問いリスト（C-3）**：DATA層に `AVOID_CHECK_QUESTIONS` を新設し、避けたい要素20項目（会社員向け8＋個人活動リスク12）それぞれに確認ポイント1つ＋質問1つを定義。結果画面の避けたい要素セクションを「避けたい要素と、確認しておくと安心なこと」に刷新し、各項目をタップすると問いが展開される。会社員ブランチは「求人票でのチェック／面接で聞いてみる」、個人活動ブランチは「始める前のチェック／経験者に聞いてみる」のラベルで表示。文言は「企業や相手を疑うためではなく、入る前・始める前に確認しておくと安心なこと」のトーンで統一し、C-2の照合プロンプトにも選択済み要素の問いを含める。
- **検証**：ACTION_GUIDE の網羅性（132/132候補、検索3・質問2、孤児キーなし）、AVOID_CHECK_QUESTIONS の網羅性（20/20項目）、照合プロンプトの内容（判断軸・避けたい要素の問い・指示文2種）をスクリプトで確認。Chrome実機（モバイル幅360px含む）で3パネルの表示・タップ展開・コピー動作を確認済み。
- **保存形式**：保存バージョンを `1.5.0` に更新。保存形式の変更はなし。

## v1.6.0 の改善点（PWA化・相談導線）

### PWA対応（D-1）

`manifest.json`・`sw.js`・アイコン（`icon-192.png` / `icon-512.png`）を追加し、ホーム画面追加とオフライン動作に対応しました。

- **プログレッシブエンハンスメント**：PWA機能はすべて追加機能として実装。`index.html` 単体を渡した場合（file://直開き・sw.jsなし）でも従来どおり全機能が動作する（SW登録は http(s) 上でのみ実行され、失敗しても無視される）。ヘッドレス環境での全機能回帰テストで確認済み。
- **キャッシュ戦略**：アプリシェル（index.html / manifest.json / アイコン2種）をinstall時にprecache。キャッシュファースト＋オフライン時のナビゲーションは index.html にフォールバック。
- **更新通知**：新しい `sw.js` を検知すると「新しいバージョンがあります（タップで更新）」のトーストを表示し、タップで新バージョンへ切り替え（作業中の勝手なリロードはしない）。**リリース時は `sw.js` の `CACHE_VERSION` を必ず更新すること。**
- **iOS Safari対応**：`apple-touch-icon`・`apple-mobile-web-app-capable`・ステータスバー設定・ホーム画面タイトルのメタタグを追加。

### ホスティング手順（GitHub Pages）

配布形式は「単一HTML」または「フォルダ／ホスティングURL」の2通りになりました。PWA機能（ホーム画面追加・オフライン）はホスティング時のみ有効です。

1. このリポジトリをGitHubへpushする
2. リポジトリの Settings → Pages → Source を「Deploy from a branch」、Branch を `main` / `(root)` に設定
3. 数分後に `https://<ユーザー名>.github.io/<リポジトリ名>/` で公開される（HTTPSのためSWが有効）
4. スマホでURLを開き「ホーム画面に追加」するとアプリとして起動できる

必要ファイル：`index.html` / `manifest.json` / `sw.js` / `icon-192.png` / `icon-512.png`（サブディレクトリ公開でも相対パスなのでそのまま動く）

### 相談導線（D-2）

- 結果画面の最下部に「もっと深く整理したい人へ」セクションを追加。「この結果を持って人に相談すると、整理が一気に進みます。信頼できる先輩・キャリアの相談相手に、エクスポートしたJSONやAI分析結果を見せてみてください」
- DATA層に `CONSULT_LINKS`（`{label, url, description}` の配列、初期値は空）を新設。運営者が相談窓口（Matcher等）を追記するとリンクボタンとして表示される。空のままならリンクは出ない。
- **保存形式**：保存バージョンを `1.6.0` に更新。保存形式の変更はなし。
